# 제안서 0.4 의존 라이브러리 (버전 pin 권장 ― Step 0-3 재현성)
# 설치 검증 후 `pip freeze > requirements.lock`으로 실제 버전 고정 권장
httpx           # HTTP 클라이언트 (동기/비동기)
pydantic>=2     # 데이터 모델·검증
pyyaml          # 설정 로딩
python-dotenv   # 환경변수
sqlalchemy>=2   # 이벤트 영구저장
pandas          # 캔들·백테스트 처리
apscheduler     # 스케줄링
tenacity        # 재시도 (지수 백오프)
loguru          # 구조화 로깅
pytest          # 테스트
