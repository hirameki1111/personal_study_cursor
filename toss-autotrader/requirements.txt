# 제안서 0.4 의존 라이브러리 ― 버전 pin(재현성)
# 최초 설치 후 `pip freeze`로 실제 설치 버전과 일치시켜 갱신할 것
httpx==0.28.1          # HTTP 클라이언트 (동기/비동기)
pydantic==2.11.7       # 데이터 모델·검증
PyYAML==6.0.2          # 설정 로딩
python-dotenv==1.1.1   # 환경변수
SQLAlchemy==2.0.41     # 이벤트 영구저장
pandas==2.3.0          # 캔들·백테스트 처리
APScheduler==3.11.0    # 스케줄링
tenacity==9.1.2        # 재시도 (지수 백오프)
loguru==0.7.3          # 구조화 로깅
pytest==8.4.1          # 테스트
